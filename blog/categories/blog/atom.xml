<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: Blog | Was ich so treibe...]]></title>
  <link href="http://uli-heller.github.com/blog/categories/blog/atom.xml" rel="self"/>
  <link href="http://uli-heller.github.com/"/>
  <updated>2013-08-07T10:00:49+02:00</updated>
  <id>http://uli-heller.github.com/</id>
  <author>
    <name><![CDATA[Uli Heller]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[Octopress - Liquid error: can’t convert nil into String]]></title>
    <link href="http://uli-heller.github.com/blog/2013/07/20/octopress-fehler/"/>
    <updated>2013-07-20T07:00:00+02:00</updated>
    <id>http://uli-heller.github.com/blog/2013/07/20/octopress-fehler</id>
    <content type="html"><![CDATA[<p>Beim Einbinden von Dateien in einen Octopress-Blog-Eintrag sehe ich immer mal wieder
Fehlermeldungen wie diese:</p>

<pre><code>Liquid error: can’t convert nil into String
</code></pre>

<p>Sie tauchen immer dann auf, wenn ich Dateien ohne Extension mittels <code>include_code</code>
einbinde:</p>

<pre><code>{{ "{% include_code redmine/passenger/passenger" }} %}
</code></pre>

<!-- more -->


<h2>Einige Tests</h2>

<h3>Wie sieht&rsquo;s mit der Version von Github/master aus?</h3>

<p>{% codeblock lang:sh %}
$ git clone <a href="https://github.com/imathis/octopress.git">https://github.com/imathis/octopress.git</a>
Klone nach &lsquo;octopress&rsquo;&hellip;
remote: Counting objects: 10661, done.
remote: Compressing objects: 100% (4588/4588), done.
remote: Total 10661 (delta 5839), reused 9830 (delta 5212)
Empfange Objekte: 100% (10661/10661), 2.30 MiB | 773.00 KiB/s, done.
Löse Unterschiede auf: 100% (5839/5839), done.
Checking connectivity&hellip; done</p>

<p>$ cd octopress/
You are using &lsquo;.rvmrc&rsquo;, it requires trusting, it is slower and it is not compatible with other ruby managers,
you can switch to &lsquo;.ruby-version&rsquo; using &lsquo;rvm rvmrc to [.]ruby-version&rsquo;
or ignore this warnings with &lsquo;rvm rvmrc warning ignore &hellip;/octopress/.rvmrc&rsquo;,
&lsquo;.rvmrc&rsquo; will continue to be the default project file in RVM 1 and RVM 2,
to ignore the warning for all files run &lsquo;rvm rvmrc warning ignore all.rvmrcs&rsquo;.</p>

<hr />

<ul>
<li>NOTICE                                                                       *</li>
</ul>


<hr />

<ul>
<li>RVM has encountered a new or modified .rvmrc file in the current directory,  *</li>
<li>this is a shell script and therefore may contain any shell commands.         *</li>
<li><pre><code>                                                                       *
</code></pre></li>
<li>Examine the contents of this file carefully to be sure the contents are      *</li>
<li>safe before trusting it!                                                     *</li>
<li>Do you wish to trust &lsquo;&hellip;/octopress/.rvmrc&rsquo;?                                 *</li>
<li>Choose v[iew] below to view the contents                                     *</li>
</ul>


<hr />

<p>y[es], n[o], v[iew], c[ancel]> yes
Using &hellip;/.rvm/gems/ruby-1.9.3-p392</p>

<p>$ bundle install
Fetching gem metadata from <a href="https://rubygems.org/........">https://rubygems.org/........</a>
Fetching gem metadata from <a href="https://rubygems.org/..">https://rubygems.org/..</a>
Resolving dependencies&hellip;
Using rake (0.9.2.2)
Using RedCloth (4.2.9)
Using chunky_png (1.2.5)
Using fast-stemmer (1.0.1)
Using classifier (1.3.3)
Using fssm (0.2.9)
Installing sass (3.2.9)
Installing compass (0.12.2)
Using directory_watcher (1.4.1)
Installing haml (3.1.7)
Installing kramdown (0.13.8)
Using liquid (2.3.0)
Using syntax (1.0.0)
Installing maruku (0.6.1)
Using posix-spawn (0.3.6)
Installing yajl-ruby (1.1.0)
Installing pygments.rb (0.3.4)
Installing jekyll (0.12.0)
Installing rack (1.5.2)
Installing rack-protection (1.5.0)
Using rb-fsevent (0.9.1)
Installing rdiscount (2.0.7.3)
Using rubypants (0.2.0)
Installing sass-globbing (1.0.0)
Installing tilt (1.3.7)
Installing sinatra (1.4.2)
Using stringex (1.4.0)
Using bundler (1.3.5)
Your bundle is complete!
Use <code>bundle show [gemname]</code> to see where a bundled gem is installed.</p>

<p>$ rake install</p>

<h2>Copying classic theme into ./source and ./sass</h2>

<p>mkdir -p source
cp -r .themes/classic/source/. source
mkdir -p sass
cp -r .themes/classic/sass/. sass
mkdir -p source/_posts
mkdir -p public</p>

<p>$ rake generate</p>

<h2>Generating Site with Jekyll</h2>

<p>directory source/stylesheets/
   create source/stylesheets/screen.css
Configuration from &hellip;/octopress/_config.yml
Building site: source &ndash;> public
Successfully generated site: source &ndash;> public</p>

<p>$ mkdir -p source/downloads/code</p>

<p>$ echo &ldquo;Uli war da&rdquo; >source/downloads/code/mytest</p>

<p>$ mkdir -p source/downloads/code/redmine/passenger</p>

<p>$ cp &hellip;/source/downloads/code/redmine/passenger/passenger source/downloads/code/redmine/passenger/.</p>

<p>{% endcodeblock %}</p>

<p>Jetzt noch ein Testdokument anlegen:</p>

<h2>{% codeblock &hellip;/source/_posts/2013-07-20-octopress.md lang:text %}</h2>

<p>layout: post
author: Uli Heller
published: yes
title: &ldquo;Octopress-Test&rdquo;
date: 2013-07-20 07:00</p>

<h2>comments: true</h2>

<p>Test:</p>

<p>{{ &ldquo;{% include_code mytest&rdquo; }} %}</p>

<p>Test2:</p>

<p>{{ &ldquo;{% include_code redmine/passenger/passenger&rdquo; }} %}
{% endcodeblock %}</p>

<p>Sichten mit</p>

<ul>
<li><code>rake preview</code></li>
<li>Browser: <a href="http://localhost:4000">http://localhost:4000</a></li>
</ul>


<p>Sieht absolut OK aus, der Fehler tritt nicht auf!</p>

<h3>Wie sieht&rsquo;s mit der Version von Github/site-2.1 aus?</h3>

<p>Mit der Version von Github/site-2.1 tritt der Fehler genauso auf wie bei mir.
Er ist dort also nicht korrigiert.</p>

<h3>Wie sieht&rsquo;s mit der Version von Github/3.0 aus?</h3>

<p>Scheint komplett andersartig zu funktionieren &ndash; komme damit momentan nicht klar.</p>

<h3>Wie sieht&rsquo;s mit der Version von Github/source-2.1 aus?</h3>

<p>Der Zweig &ldquo;source-2.1&rdquo; existiert auf Github nicht mehr!</p>

<h2>Weiteres Vorgehen</h2>

<p>Für mich sieht&rsquo;s so aus, als wäre das Aufsetzen auf &ldquo;source-2.1&rdquo; eine echt blöde
Entscheidung gewesen. Werde mein Octopress wohl in nächster Zeit umstellen müssen
auf &ldquo;master&rdquo;.</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Octopress-Aktualisierung 1/2 - Grundtests]]></title>
    <link href="http://uli-heller.github.com/blog/2013/03/11/octopress-aktualisierung/"/>
    <updated>2013-03-11T08:00:00+01:00</updated>
    <id>http://uli-heller.github.com/blog/2013/03/11/octopress-aktualisierung</id>
    <content type="html"><![CDATA[<h1>Aktualisierung meiner Octopress-Installation (&frac12;)</h1>

<p>Vor grob 5 Monaten habe ich Octopress erstmalig aufgesetzt und seitdem die
Finger von den Aktualisierungen gelassen. Dadurch funktionieren in &ldquo;meiner&rdquo; Installation
nun gewisse Dinge nicht ganz so gut wir im Original. Beispielsweise soll die Verlinkung
von Blog-Posts im Original über einen Methodenaufruf funktionieren, bei meiner Variante
muß ich mir den Link mühsam zusammenklauben.</p>

<p>Erstmal möchte ich testen, ob die aktualisierte Version überhaupt funktioniert. Diese Tests
mache ich mit &ldquo;master&rdquo; und &ldquo;source-2.1&rdquo;.</p>

<!-- more -->


<h2>Aktuelle Octopress-Version runterladen</h2>

<p>{% codeblock Octropress runterladen lang:sh %}
git clone git://github.com/imathis/octopress.git octopress
cd octopress
git branch -a
  * master</p>

<pre><code>remotes/origin/2.1
remotes/origin/HEAD -&gt; origin/master
remotes/origin/colorize
remotes/origin/gh-pages
remotes/origin/guard
remotes/origin/linklog
remotes/origin/master
remotes/origin/migrator
remotes/origin/refactor_with_tests
remotes/origin/rubygemcli
remotes/origin/site
remotes/origin/site-2.1
</code></pre>

<p>{% endcodeblock %}</p>

<h2>Kurztest Octopress-2.0</h2>

<p>{% codeblock Kurztest Octropress-2.0 lang:sh %}
cd octopress
gem install bundler
bundle install
rake install
rake generate
rake preview
{% endcodeblock %}</p>

<p>&hellip; danach dann im Browser den Link <a href="http://localhost:4000">http://localhost:4000</a> öffnen &ndash; es
wird die Octopress-Grundseite angezeigt.</p>

<h2>Kurztest Octopress-2.1</h2>

<p>{% codeblock Kurztest Octropress-2.1 &ndash; Mecker wegen JavaScript runtime lang:sh %}
cd octopress
git checkout 2.1
gem install bundler
bundle install
rake install
  rake aborted!
  Could not find a JavaScript runtime. See <a href="https://github.com/sstephenson/execjs">https://github.com/sstephenson/execjs</a> for a list of available runtimes.
  /home/uli/.rvm/gems/ruby-1.9.3-p327/gems/execjs-1.4.0/lib/execjs/runtimes.rb:51:in <code>autodetect'
  /home/uli/.rvm/gems/ruby-1.9.3-p327/gems/execjs-1.4.0/lib/execjs.rb:5:in</code>&lt;module:ExecJS>&lsquo;
  /home/uli/.rvm/gems/ruby-1.9.3-p327/gems/execjs-1.4.0/lib/execjs.rb:4:in <code>&lt;top (required)&gt;'
  /home/uli/.rvm/gems/ruby-1.9.3-p327/gems/uglifier-1.2.6/lib/uglifier.rb:3:in</code>require&rsquo;
  /home/uli/.rvm/gems/ruby-1.9.3-p327/gems/uglifier-1.2.6/lib/uglifier.rb:3:in <code>&lt;top (required)&gt;'
  /home/uli/tmp/o/octopress/lib/octopress/js_asset_manager.rb:4:in</code>require'
  /home/uli/tmp/o/octopress/lib/octopress/js_asset_manager.rb:4:in <code>&lt;top (required)&gt;'
  /home/uli/tmp/o/octopress/lib/octopress.rb:5:in</code>require'
  /home/uli/tmp/o/octopress/lib/octopress.rb:5:in <code>&lt;top (required)&gt;'
  /home/uli/tmp/o/octopress/Rakefile:9:in</code>require'
  /home/uli/tmp/o/octopress/Rakefile:9:in <code>&lt;top (required)&gt;'
  /home/uli/.rvm/gems/ruby-1.9.3-p327/bin/ruby_noexec_wrapper:14:in</code>eval'
  /home/uli/.rvm/gems/ruby-1.9.3-p327/bin/ruby_noexec_wrapper:14:in `<main>&lsquo;
  (See full trace by running task with &mdash;trace)
{% endcodeblock %}</p>

<p>&hellip; funktioniert also nicht so richtig.</p>

<p>Am einfachsten korrigiert man&rsquo;s durch Anpassung vom Gemfile:</p>

<p>{% codeblock Anpassungen am Gemfile lang:diff %}
diff &mdash;git a/Gemfile b/Gemfile
@@ -15,6 +15,7 @@
 group :development do
   gem &lsquo;tzinfo&rsquo;, &lsquo;~> 0.3.35&rsquo;
   gem &lsquo;stitch-rb&rsquo;
   gem &lsquo;uglifier&rsquo;
+  gem &lsquo;therubyracer&rsquo;</p>

<p>   # Guard related
   gem &lsquo;guard&rsquo;
{% endcodeblock %}</p>

<p>Danach dann <code>bundle install</code> und alles wird gut! Nach <code>rake install</code>, <code>rake generate</code>
und <code>rake preview</code> kann man wie üblich
im Browser den Link <a href="http://localhost:4000">http://localhost:4000</a> öffnen und es
wird die Octopress-Grundseite angezeigt.</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Octopress-2.1]]></title>
    <link href="http://uli-heller.github.com/blog/2012/11/24/octopress-21/"/>
    <updated>2012-11-24T10:49:00+01:00</updated>
    <id>http://uli-heller.github.com/blog/2012/11/24/octopress-21</id>
    <content type="html"><![CDATA[<h1>Meine Tests mit Octopress-2.1</h1>

<p>Ausgangspunkt ist meine &ldquo;normale&rdquo; Octopress-Installation:</p>

<p>{% codeblock Normale Octopress-Installation lang:sh %}
octopress$ git branch -a
  master
* source
  remotes/octopress/2.1
  remotes/octopress/gh-pages
  remotes/octopress/linklog
  remotes/octopress/master
  remotes/octopress/refactor_with_tests
  remotes/octopress/rubygemcli
  remotes/octopress/site
  remotes/origin/HEAD &ndash;> origin/master
  remotes/origin/gh-pages
  remotes/origin/master
  remotes/origin/source
{% endcodeblock %}</p>

<p>Mit diesem Setup kann man nun umschalten auf Version 2.1:</p>

<p>{% codeblock Umstellen auf Octopress-2.1 lang:sh %}
octopress$ export LANG=C.UTF-8
octopress$ git checkout -t remotes/octopress/2.1
Branch 2.1 set up to track remote branch 2.1 from octopress by rebasing.
Switched to a new branch &lsquo;2.1&rsquo;
octopress$ bundle install
octopress$ rake install
octopress$ rake generate
octopress$ rake preview</p>

<h1>Browser: <a href="http://localhost:4000">http://localhost:4000</a></h1>

<p>{% endcodeblock %}</p>

<p>Hier meine ersten Eindrücke:</p>

<ul>
<li>Code-Blöcke verwenden nun weißen Hintergrund, was für mich deutlich lesbarer und ansprechender aussieht</li>
<li>Blog-Einträge mit <code>published: false</code> werden nicht veröffentlicht, in 2.0 werden alle Blog-Einträge veröffentlicht</li>
<li>Bei RVM wird Ruby-1.9.2 &ldquo;angezogen&rdquo;, es funktioniert aber auch mit 1.9.3</li>
<li>Beim Drücken auf &ldquo;Blog&rdquo; erscheint in der Leiste rechts keine Liste der Blog-Einträge &ndash; source/_includes/sidebars/blog_index_default.html</li>
<li>Bei Überschriften werden die ersten Buchstaben immer groß geschrieben (also: So Eine Überschrift) &ndash; _config.yml, titlecase</li>
<li>Datumsangaben erfolgen in Englisch (wie bei 2.0) &ndash; etwas kompliziert zu ändern</li>
</ul>


<p>{% codeblock Anpassungen an Octopress-2.1 lang:sh %}
git branch -m 2.1 source-2.1
git checkout source-2.1
git push -u origin source-2.1
{% endcodeblock %}</p>

<h2>Eigenes Darstellungsthema erzeugen</h2>

<h3>Kopie des Standards-Themas anlegen</h3>

<p>{% codeblock Eigenes Darstellungsthema für Octopress-2.1 lang:sh %}
cp -a .themes/classic .themes/uli
git add .themes/uli
git commit -m &ldquo;.themes: classic &ndash;> uli&rdquo;
{% endcodeblock %}</p>

<h3>Anpassungen von Octopress-2.0 übernehmen</h3>

<ul>
<li>Historie vom Zweig &ldquo;source&rdquo; durchsuchen nach den dort erledigten Änderungen am Thema</li>
<li>Übernehmen der Änderungen in den Zweig &ldquo;source-2.1&rdquo;</li>
</ul>


<p>{% codeblock Anpassungen des Darstellungsthemas von Octopress-2.0 übernehmen lang:sh %}</p>

<h1>Sichten der Historie</h1>

<p>octopress$ git log &mdash;oneline ..source &mdash; .themes/uli
b941c1c Anpassungen am Layout
1cfd652 cp -a .themes/classic .themes/uli</p>

<h1>Änderungen in source-2.1 übernehmen</h1>

<p>octopress$ git rebase -i &mdash;onto source-2.1 1cfd652 b941c1c</p>

<h1>Konflikt wird angezeigt &ndash;> auflösen und festschreiben (committen)</h1>

<p>octopress$ &hellip;</p>

<h1>Zustand sichten</h1>

<p>octopress$ gitk</p>

<h1>&ldquo;Detached head&rdquo;, source-2.1 liegt ein wenig &ldquo;zurück&rdquo;</h1>

<p>octopress$ git checkout -b tmp
octopress$ git checkout -B source-2.1 tmp
octopress$ git branch -d tmp
{% endcodeblock %}</p>

<h3>Darstellungsthema aktivieren</h3>

<p>{% codeblock Anpassungen des Darstellungsthemas von Octopress-2.0 übernehmen lang:sh %}
octopress$ rake install[&lsquo;uli&rsquo;]
A theme is already installed, proceeding will overwrite existing files. Are you sure? [y/n] y</p>

<h2>Copying uli theme into ./source and ./sass</h2>

<p>mkdir -p source
cp -r .themes/uli/source/. source
mkdir -p sass
cp -r .themes/uli/sass/. sass
mkdir -p source/_posts
mkdir -p public
{% endcodeblock %}</p>

<h2>Blog-Einträge von Octopress-2.0 übernehmen</h2>

<p>{% codeblock Dokumente von Octopress-2.0 übernehmen lang:sh %}
git add source
git commit -m &ldquo;Quelltexte in&rsquo;s Repository aufgenommen&rdquo; .
git checkout source &mdash; ./source/_posts
git commit -m &ldquo;Blog-Einträge kopiert von &lsquo;source&rsquo;&rdquo; ./source
{% endcodeblock %}</p>

<h2>Einstellungen anpassen</h2>

<h3>Grundeinstellungen</h3>

<p>{% codeblock Einstellungen anpassen &ndash; _config.yml lang:diff %}
@@ -2,10 +2,10 @@
 #      Main Configs       #
 # &mdash;&mdash;&mdash;&mdash;&mdash;&mdash;&mdash;&mdash;&mdash;&mdash;&mdash;&ndash; #</p>

<p>-url: <a href="http://yoursite.com">http://yoursite.com</a>
-title: My Octopress Blog
-subtitle: A blogging framework for hackers.
-author: Your Name
+url: <a href="http://uli-heller.github.com">http://uli-heller.github.com</a>
+title: Was ich so treibe&hellip;
+subtitle: Java, Linux und Groovy
+author: Uli Heller
 simple_search: <a href="http://google.com/search">http://google.com/search</a>
 description:
{% endcodeblock %}</p>

<h3>Überschriften &ndash; keine Großbuchstaben</h3>

<p>{% codeblock Keine Großbuchstaben in Überschriften &ndash; _config.yml lang:diff %}
@@ -49,7 +49,7 @@
 linklog_marker_position: after
 linklog_marker_position_feed: after
 standard_post_marker:</p>

<p>-titlecase: true       # Converts page and post titles to titlecase
+titlecase: false       # Converts page and post titles to titlecase</p>

<p> # To change the layout&rsquo;s default sidebar Add a new sidebar source/_includes/sid
 # then make changes below, eg. post_sidebar: your_sidebar.html
{% endcodeblock %}</p>

<h3>Deutsche Übersetzungen</h3>

<p>Die Ideen sind weitgehend &ldquo;geklaut&rdquo; von <a href="http://www.uru.ch/blog/2011/09/12/octopress-oberflaeche-im-schnelldurchgang-auf-deutsch-uebersetzen/">Roger Schärer</a>. In folgenden Dateien sind englische Texte hinterlegt:</p>

<ul>
<li>_config.yml &hellip; OK</li>
<li>source/_includes/archive_post.html &hellip; OK</li>
<li>source/<em>includes/asides/recent_posts.html &hellip; nicht gefunden
source/</em>includes/sidebars/sections/recent_posts.html</li>
<li>source/<em>includes/asides/twitter.html .. nicht gefunden
source/</em>includes/sidebars/sections/twitter.html</li>
<li>source/_includes/custom/navigation.html</li>
<li>source/_includes/navigation.html</li>
<li>source/_includes/post/author.html</li>
<li>source/_layouts/post.html</li>
<li>source/_layouts/posts.html &hellip; nicht gefunden</li>
<li>source/_layouts/page.html &hellip; nicht gefunden</li>
<li>source/index.html</li>
<li>source/blog/archives/index.html &hellip; nicht gefunden
source/archives/index.html</li>
</ul>


<p>Die meisten dieser Änderungen führe ich nicht direkt an den Dateien unterhalb von &ldquo;source&rdquo; durch, sondern in meinem Themenverzeichnis &ldquo;.themes/uli&rdquo;. Von dort kann ich sie dann mittels <code>rake install['uli']</code> aktivieren.</p>

<h3>Deutsche Datumsangaben</h3>

<p>Das grundätzliche Vorgehen kommt von <a href="http://home-server-mind.com/deutsche-lokalisation-in-octopress-seiten/">Marc </a>. Er schreibt allerdings, dass man als Einstellung für das Format &ldquo;ordinal&rdquo; verwenden muß. Grund hierfür ist eine Unschönheit in seiner Implementierung der Methode &ldquo;format_date&rdquo;. Bei meiner Variante kann man auch beliebige andere Einstellungen verwenden. Ich verwende beispielsweise &ldquo;%d. %B %Y&rdquo;.</p>

<p>{% codeblock Deutsche Datumsangaben &ndash; plugins/date.rb lang:diff %}
@@ -1,5 +1,57 @@
+# &ndash;<em>&ndash; coding: utf-8 &ndash;</em>&ndash;
 module Octopress
   module Date
+    #Deutsche Lokalisation:
+    MONTHNAMES_DE = [nil,
+      &ldquo;Januar&rdquo;, &ldquo;Februar&rdquo;, &ldquo;März&rdquo;, &ldquo;April&rdquo;, &ldquo;Mai&rdquo;, &ldquo;Juni&rdquo;,
+      &ldquo;Juli&rdquo;, &ldquo;August&rdquo;, &ldquo;September&rdquo;, &ldquo;Oktober&rdquo;, &ldquo;November&rdquo;, &ldquo;Dezember&rdquo; ]
+    ABBR_MONTHNAMES_DE = [nil,
+      &ldquo;Jan&rdquo;, &ldquo;Feb&rdquo;, &ldquo;Mär&rdquo;, &ldquo;Apr&rdquo;, &ldquo;Mai&rdquo;, &ldquo;Jun&rdquo;,
+      &ldquo;Jul&rdquo;, &ldquo;Aug&rdquo;, &ldquo;Sep&rdquo;, &ldquo;Okt&rdquo;, &ldquo;Nov&rdquo;, &ldquo;Dez&rdquo; ]
+    DAYNAMES_DE = [
+      &ldquo;Sonntag&rdquo;, &ldquo;Montag&rdquo;, &ldquo;Dienstag&rdquo;, &ldquo;Mittwoch&rdquo;,
+      &ldquo;Donnerstag&rdquo;, &ldquo;Freitag&rdquo;, &ldquo;Samstag&rdquo; ]
+    ABBR_DAYNAMES_DE = [
+      &ldquo;So&rdquo;, &ldquo;Mo&rdquo;, &ldquo;Di&rdquo;, &ldquo;Mi&rdquo;,
+      &ldquo;Do&rdquo;, &ldquo;Fr&rdquo;, &ldquo;Sa&rdquo; ]
+
+    # Returns a datetime if the input is a string
+    def datetime(date)
+      if date.class == String
+        date = Time.parse(date)
+      end
+      date
+    end
+
+    # in _config.yml muss stehen: date_format: ordinal
+    def ordinalize(date)
+      #**** hier Format bei Bedarf ändern, z.B. %A für ausgeschriebenen Wochent
+      format_date(date, &ldquo;%a, %e. %B %Y&rdquo;) # SA, 10. MÄRZ 2012
+    end
+
+
+    # Formats date either as ordinal or by given date format
+    # Adds %o as ordinal representation of the day
+    def format_date(date, format)
+      myformat = format.dup
+      date = datetime(date)
+      if format.nil? || format.empty? || format == &ldquo;ordinal&rdquo;
+        date_formatted = ordinalize(date)
+      else
+        myformat.gsub!(/%a/, ABBR_DAYNAMES_DE[date.wday])
+        myformat.gsub!(/%A/, DAYNAMES_DE[date.wday])
+        myformat.gsub!(/%b/, ABBR_MONTHNAMES_DE[date.mon])
+        myformat.gsub!(/%B/, MONTHNAMES_DE[date.mon])
+        date_formatted = date.strftime(myformat)
+        # date_formatted = date.strftime(format)
+        # date_formatted.gsub!(/%o/, ordinal(date.strftime(&lsquo;%e&rsquo;).to_i))
+      end
+      date_formatted #+&ldquo; ULI:#{date.mon}&rdquo;
+    end
+
+  end
+
+  module DateEn</p>

<pre><code> # Returns a datetime if the input is a string
 def datetime(date)
</code></pre>

<p>@@ -95,4 +147,11 @@ module Jekyll</p>

<pre><code>   self.data['updated_formatted'] = format_date(self.data['updated'], date_format) if self.data.has_key?('updated')
 end
</code></pre>

<p>   end
+
+  module Filters
+    include Octopress::Date
+    def date(date, format)
+      format_date(date, format)
+    end
+  end
{% endcodeblock %}</p>

<p>Änderung an _config.yml, damit das Datum so angegeben wird: &ldquo;25. Dezember 2012&rdquo;</p>

<p>{% codeblock Datumsangaben &lsquo;TT. Monat JJJJ&rsquo; &ndash; _config.yml lang:diff %}
@@ -13,7 +13,11 @@ description:
 # You can customize the format as defined in
 # <a href="http://www.ruby-doc.org/core-1.9.2/Time.html#method-i-strftime">http://www.ruby-doc.org/core-1.9.2/Time.html#method-i-strftime</a>
 # Additionally, %o will give you the ordinal representation of the day
-date_format: &ldquo;ordinal&rdquo;
+#  %d.%m.%Y" &ndash;> 25.12.2012
+#  %d. %B %Y" &ndash;> 25. December 2012 (oder 25. Dezember 2012)
+date_format: &ldquo;%d. %B %Y&rdquo;</p>

<p> # RSS / Email (optional) subscription links (change if using something like Fee
 subscribe_rss: /atom.xml
{% endcodeblock %}</p>

<h2>GitHub-Pages reaktivieren</h2>

<p>{% codeblock GutHub-Pages reaktivieren &ndash; Rakefile lang:diff %}
@@ -9,13 +9,13 @@
 ssh_user       = &ldquo;<a href="&#x6d;&#97;&#105;&#108;&#116;&#111;&#x3a;&#x75;&#115;&#x65;&#114;&#x40;&#x64;&#111;&#x6d;&#x61;&#105;&#110;&#x2e;&#99;&#111;&#x6d;">&#x75;&#115;&#x65;&#x72;&#64;&#x64;&#x6f;&#x6d;&#97;&#105;&#x6e;&#46;&#99;&#x6f;&#109;</a>&rdquo;
 ssh_port       = &ldquo;22&rdquo;
 document_root  = &ldquo;~/website.com/&rdquo;
 rsync_delete   = false
-deploy_default = &ldquo;rsync&rdquo;
+deploy_default = &ldquo;push&rdquo;</p>

<p> # Hidden &ldquo;dot&rdquo; files that should be included with the deployed site (see task c
 copy_dot_files = []</p>

<p> # This will be configured for you when you run config_deploy
-deploy_branch  = &ldquo;gh-pages&rdquo;
+deploy_branch  = &ldquo;master&rdquo;</p>

<p> ## &mdash; Misc Configs &mdash; ##
{% endcodeblock %}</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Octopress-Installation]]></title>
    <link href="http://uli-heller.github.com/blog/2012/11/24/octopress/"/>
    <updated>2012-11-24T10:49:00+01:00</updated>
    <id>http://uli-heller.github.com/blog/2012/11/24/octopress</id>
    <content type="html"><![CDATA[<h1>Meine erste Octopress-Installation</h1>

<h2>Installation</h2>

<h3>Vorbereitungen</h3>

<ul>
<li>Sicherstellen: Zugang zum Internet besteht</li>
<li>Größe der /usr-Partition auf 800MB setzen</li>
<li>Größe der /tmp-Partition auf 300MB setzen</li>
<li>Größe der /home-Partition auf 250MB setzen</li>
<li>GCC installieren</li>
<li>Libs installieren</li>
<li>Curl installieren</li>
</ul>


<p>{% codeblock Partition vergrößern und diverse Pakete installieren lang:sh %}
sudo lvextend -L+400M /dev/systemvg/usrlv
sudo resize2fs /dev/systemvg/usrlv
sudo lvextend -L+200M /dev/systemvg/tmplv
sudo resize2fs /dev/systemvg/tmplv
sudo lvextend -L+200M /dev/systemvg/homelv
sudo resize2fs /dev/systemvg/homelv
sudo apt-get install gcc build-essential
sudo apt-get install libyaml-dev libz-dev libssl-dev
sudo apt-get install curl
{% endcodeblock %}</p>

<h3>Basispakete</h3>

<h4>Git</h4>

<p>{% codeblock Git installieren lang:sh %}
sudo apt-get install -y git
sudo apt-get clean
{% endcodeblock %}</p>

<p>Für GIT wird die Version 1.8.0-0dp3~precise01 vom DPREPO angezogen.</p>

<h4>Ruby mittels RVM</h4>

<p>{% codeblock Ruby installieren lang:sh %}
curl -L <a href="https://get.rvm.io">https://get.rvm.io</a> | bash -s stable &mdash;ruby
. ./.rvm/scripts/rvm
rvm install 1.9.3 # Zeigt eine Fehlermeldung, weil 1.9.3 bereits installiert ist
rvm use 1.9.3
rvm rubygems latest
ruby &mdash;version    # ruby 1.9.3p327 (2012-11-10 revision 37606) [i686-linux]
{% endcodeblock %}</p>

<h3>Octopress</h3>

<h4>Erstinstallation</h4>

<p>Die Schritte in diesem Abschnitt werden nur bei der allerersten Installation durchgeführt. Die dabei gemachten Einstellungen werden in einem zentralen Speicherbereich auf GitHub abgelegt und bei nachfolgenden Installationen von dort geladen.</p>

<h5>Octopress und Pakete</h5>

<p>{% codeblock Octropress installieren lang:sh %}
git clone git://github.com/imathis/octopress.git octopress
cd octopress
gem install bundler
bundle install
rake install
{% endcodeblock %}</p>

<h5>Textile statt Markdown</h5>

<p>{% codeblock Textile statt Markdown &ndash; Rakefile lang:diff %}
@@ -22,8 +22,8 @@ deploy_dir      = &ldquo;<em>deploy&rdquo;   # deploy directory (for Github p
 stash_dir       = &ldquo;</em>stash&rdquo;    # directory to stash posts for speedy generation
 posts_dir       = &ldquo;_posts&rdquo;    # directory for blog files
 themes_dir      = &ldquo;.themes&rdquo;   # directory for blog files
-new_post_ext    = &ldquo;markdown&rdquo;  # default new post file extension when using the
-new_page_ext    = &ldquo;markdown&rdquo;  # default new page file extension when using the
+new_post_ext    = &ldquo;textile&rdquo;   # default new post file extension when using the
+new_page_ext    = &ldquo;textile&rdquo;   # default new page file extension when using the
 server_port     = &ldquo;4000&rdquo;      # port for preview server eg. localhost:4000
{% endcodeblock %}</p>

<h5>Autor und Titel</h5>

<p>{% codeblock Autor und Tutel &ndash; _config.yml lang:diff %}
@@ -2,10 +2,10 @@
 #      Main Configs       #
 # &mdash;&mdash;&mdash;&mdash;&mdash;&mdash;&mdash;&mdash;&mdash;&mdash;&mdash;&ndash; #</p>

<p>-url: <a href="http://yoursite.com">http://yoursite.com</a>
-title: My Octopress Blog
-subtitle: A blogging framework for hackers.
-author: Your Name
+url: <a href="http://uli-heller.github.com">http://uli-heller.github.com</a>
+title: Was ich so treibe&hellip;
+subtitle: Linux, Java und Groovy
+author: Uli Heller
 simple_search: <a href="http://google.com/search">http://google.com/search</a>
 description:
{% endcodeblock %}</p>

<h4>Veröffentlichung auf GitHub</h4>

<p>Ziel: Die Octopress-Seiten sollen auf GitHub unter</p>

<ul>
<li><a href="http://uli-heller.github.com/">http://uli-heller.github.com/</a></li>
</ul>


<p>veröffentlicht werden.</p>

<h5>Vorbereitungen auf GitHub</h5>

<p>Auf GitHub muß ein spezielles Repository angelegt werden:</p>

<ul>
<li>mein GitHub-Benutzer: &ldquo;uli-heller&rdquo;</li>
<li>neu anzulegendes Repository: &ldquo;uli-heller.github.com&rdquo;</li>
</ul>


<p>In der Liste der eigenen Repositories erscheint dann:</p>

<ul>
<li>uli-heller/uli-heller.github.com</li>
</ul>


<p>Test: Lege in dem Master-Zweig dieses Repositories eine Datei mit Namen</p>

<ul>
<li>index.html</li>
</ul>


<p>an und gib' danach im Browser die URL</p>

<ul>
<li><a href="http://uli-heller.github.com">http://uli-heller.github.com</a></li>
</ul>


<p>ein. Mit etwas Glück erscheint der Inhalt der &ldquo;index.html&rdquo;</p>

<h5>Anpassungen im Octopress-Verzeichnis</h5>

<p>{% codeblock Octopress für Veröffentlichung vorbereiten lang:sh %}
cd octopress
rake setup_github_pages</p>

<h1>Repository url: git@github.com:uli-heller/uli-heller.github.com</h1>

<p>rake generate
rake deploy
{% endcodeblock %}</p>

<h5>Quellverzeichnis zur Sicherung hinzufügen</h5>

<p>{% codeblock Sicherung vorbereiten lang:sh %}
git add source
git commit -m &ldquo;Quelltexte sichern&rdquo;
{% endcodeblock %}</p>

<h5>Neue Artikel sichern</h5>

<p>Neue Artikel werden mit diesem Befehl auf GitHub gesichert:</p>

<p>{% codeblock Quellverzeichnis sichern lang:sh %}
cd octopress
git commit -m &ldquo;Sprechende Nachricht mit Änderungsbeschreibungen&rdquo;
git push origin source
{% endcodeblock %}</p>

<h5>Neue Veröffentlichungen</h5>

<p>Immer dann, wenn neue Artikel veröffentlicht werden sollen, muß man so vorgehen:</p>

<p>{% codeblock Neue Veröffentlichungen lang:sh %}
cd octopress
rake generate
rake deploy
{% endcodeblock %}</p>

<h4>Folgeinstallation</h4>

<p>{% codeblock Folgeinstallation lang:sh %}
git clone  git@github.com:uli-heller/uli-heller.github.com octopress
cd octopress
git checkout source
gem install bundler
bundle install
rake install[&lsquo;uli&rsquo;]
git remote add octopress git://github.com/imathis/octopress.git
git fetch octopress</p>

<h1>&hellip; nachfolgend nur für GitHub-Veröffentlichungen</h1>

<p>rm -rf <em>deploy
git init </em>deploy
cd _deploy
git remote add -t master -f origin git@github.com:uli-heller/uli-heller.github.com
git checkout master
cd ..
{% endcodeblock %}</p>

<h2>Anpassungen</h2>

<h3>Darstellung</h3>

<p>{% codeblock Meinen Darstellungsstil aktivieren lang:sh %}
cd octopress
rake install[&lsquo;uli&rsquo;]
{% endcodeblock %}</p>

<p>Zurück zur Standard-Darstellung geht&rsquo;s so:</p>

<p>{% codeblock Standarddarstellung aktivieren lang:sh %}
cd octopress
rake install
{% endcodeblock %}</p>

<h2>Ein erster Blog-Eintrag</h2>

<p>{% codeblock Ersten Blog-Eintrag erstellen lang:sh %}
$ rake new_post[&ldquo;Octopress&rdquo;]
$ mkdir -p source/<em>posts
Creating new post: source/</em>posts/2012-11-10-octopress.textile</p>

<h1>Datei source/_posts/2012-11-10-octopress.textile editieren</h1>

<p>$ rake generate
$ rake preview</p>

<h1>Sichten mit <a href="http://localhost:4000">http://localhost:4000</a></h1>

<p>$ git commit -m &ldquo;Erster Blog-Eintrag ist fertig&rdquo;
$ git push origin source
{% endcodeblock %}</p>

<h2>Probleme</h2>

<h3>Offene Probleme</h3>

<ul>
<li>Wie markiere ich in Blog-Einträgen am besten nachträgliche Änderungen?</li>
<li>Wie verwende ich include_code in Verbindung mit Textile?

<ul>
<li><a href="https://github.com/imathis/octopress/issues/500">https://github.com/imathis/octopress/issues/500</a></li>
<li><code>git branch -a --contains ca47041</code></li>
</ul>
</li>
</ul>


<h3>Erledigte Probleme</h3>

<h4>Farben sind recht duster</h4>

<p>Die Beschreibung ist obsolet! Verwende besser: <code>rake install['uli']</code>!</p>

<p>{% codeblock Hellere Farben &ndash; sass/custom/<em>colors.scss lang:diff %}
uli@uli-hp:~/git/octopress$ diff -u sass/custom/</em>colors.scss~ sass/custom/<em>colors.scss
&mdash;&ndash; sass/custom/</em>colors.scss~   2012-11-10 19:45:17.000000000 +0100
+++ sass/custom/_colors.scss    2012-11-18 11:21:14.913856809 +0100
@@ -41,3 +41,61 @@
 //$pre-bg: $base03;
 //$pre-border: darken($base02, 5);
 //$pre-color: $base1;
+
+// 2012-11-18
+// <a href="http://aijazansari.com/2012/08/27/how-to-customize-octopress-theme/">http://aijazansari.com/2012/08/27/how-to-customize-octopress-theme/</a>
+// ////////////////////////////////////////
+// change the background colors
+//
+html {
+  background: #fff !important;
+}
+body {
+  > div {
+    background: #fff !important;
+    > div {
+      background: #fff !important;
+      }
+  }
+}
+$main-bg: #ffffff !default;
+$page-bg: #ffffff !default;
+
+// ////////////////////////////////////////
+// change the header colors
+//
+$header-bg: #fff;
+$title-color: #000000 !default;
+$text-color: #333 !default;
+$text-color-light: #777 !default;
+$subtitle-color: darken($header-bg, 58);
+
+// ////////////////////////////////////////
+// change the nav bar
+//
+$nav-bg: #fff;
+$nav-bg-front: #fff;
+$nav-bg-back: #fff;
+$nav-color: darken($nav-bg, 78) !default;
+$nav-color-hover: darken($nav-color, 25) !default;
+$nav-border: darken($nav-bg, 50) !default;
+$nav-border-top: darken($nav-bg, 33) !default;
+$nav-border-bottom: darken($nav-bg, 33) !default;
+$nav-border-left: darken($nav-bg, 11) !default;
+$nav-border-right: lighten($nav-bg, 7) !default;
+
+
+// ////////////////////////////////////////
+// change the footer
+//
+$footer-color: #888 !default;
+$footer-bg: #fff !default;
+$footer-bg-front: #fff !default;
+$footer-bg-back: #fff !default;
+$footer-color: darken($footer-bg, 38) !default;
+$footer-color-hover: darken($footer-color, 10) !default;
+$footer-border-top: lighten($footer-bg, 15) !default;
+$footer-border-bottom: darken($footer-bg, 15) !default;
+$footer-link-color: darken($footer-bg, 38) !default;
+$footer-link-color-hover: darken($footer-color, 25) !default;
+$page-border-bottom: darken($footer-bg, 5) !default;
{% endcodeblock %}</p>

<h4>Listen sind nicht eingerückt</h4>

<p>Die Beschreibung ist obsolet! Verwende besser: <code>rake install['uli']</code>!</p>

<p>Die Darstellung von Listen ist unschön. Der Fehler betrifft Listen wie diese:</p>

<ul>
<li>Erster Listeneintrag</li>
<li>Zweiter Listeneintrag</li>
</ul>


<p>Die Listeneinträge werden zu weit links angeziegt, so dass die &ldquo;Punkte&rdquo; links außerhalb des Textbereichs erscheinen.</p>

<p>Korrektur: Editiere sass/custom/_layout.scss wie folgt:</p>

<p>{% codeblock Eingerückte Listen &ndash; sass/custom/<em>layout.scss lang:diff %}
diff -u sass/custom/</em>layout.scss~  sass/custom/<em>layout.scss
&mdash;&ndash; sass/custom/</em>layout.scss~   2012-11-10 19:45:17.000000000 +0100
+++ sass/custom/_layout.scss    2012-11-18 11:05:43.661813157 +0100
@@ -6,7 +6,7 @@
 //$header-padding-bottom: 1.5em;</p>

<p> //$max-width: 1350px;
&ndash;//$indented-lists: true;
+$indented-lists: true;</p>

<p> // Padding used for layout margins
 //$pad-min: 18px;
{% endcodeblock %}</p>

<h4>Ruby mittels RBENV</h4>

<p>Funktioniert nicht, wir verwenden stattdessen RVM!</p>

<h5>RBENV</h5>

<p>{% codeblock RBENV lang:sh %}
cd
git clone git://github.com/sstephenson/rbenv.git .rbenv
echo &lsquo;export PATH=&ldquo;$HOME/.rbenv/bin:$PATH&rdquo;&rsquo; >> ~/.bash_profile
echo &lsquo;eval &ldquo;$(rbenv init &ndash;)&rdquo;&rsquo; >> ~/.bash_profile
git clone git://github.com/sstephenson/ruby-build.git ~/.rbenv/plugins/ruby-build
source ~/.bash_profile
{% endcodeblock %}</p>

<h5>RUBY</h5>

<p>{% codeblock RUBY lang:sh %}
rbenv install 1.9.3-p0
rbenv rehash
{% endcodeblock %}</p>

<p>Danach sollte dann <code>ruby --version</code> irgendwas mit 1.9.3 anzeigen, funktioniert aber nicht!</p>

<h4>YAML und zlib</h4>

<p>{% codeblock Fehlermeldung yaml und zlib lang:sh %}
$ gem install bundler
/home/uli/.rvm/rubies/ruby-1.9.3-p327/lib/ruby/1.9.1/yaml.rb:56:in `<top (required)>&lsquo;:
It seems your ruby installation is missing psych (for YAML output).
To eliminate this warning, please install libyaml and reinstall your ruby.
ERROR:  Loading command: install (LoadError)</p>

<pre><code>cannot load such file -- zlib
</code></pre>

<p>ERROR:  While executing gem &hellip; (NameError)</p>

<pre><code>uninitialized constant Gem::Commands::InstallCommand
</code></pre>

<p>{% endcodeblock %}</p>

<p>Lösungsversuch mit libyaml-dev und libz-dev:</p>

<p>{% codeblock yaml und zlib installieren lang:sh %}
sudo apt-get install libyaml-dev libz-dev
rvm reinstall 1.9.3
cd octopress
gem install bundler
{% endcodeblock %}</p>

<p>&hellip; funktioniert.</p>

<h4>OpenSSL</h4>

<p>{% codeblock Fehlermeldung OpenSSL lang:sh %}
uli@ubuntu1204:~/octopress$ rake generate</p>

<h2>Generating Site with Jekyll</h2>

<p>directory source/stylesheets/
   create source/stylesheets/screen.css
/home/uli/.rvm/gems/ruby-1.9.3-p327/gems/maruku-0.6.0/lib/maruku/input/parse_doc.rb:22:in <code>&lt;top (required)&gt;': iconv will be deprecated in the future, use String#encode instead.
Configuration from /home/uli/octopress/_config.yml
/home/uli/.rvm/rubies/ruby-1.9.3-p327/lib/ruby/1.9.1/net/https.rb:22:in</code>require': cannot load such file &mdash; openssl (LoadError)</p>

<pre><code>from /home/uli/.rvm/rubies/ruby-1.9.3-p327/lib/ruby/1.9.1/net/https.rb:22:in `&lt;top (required)&gt;'
</code></pre>

<p>{% endcodeblock %}</p>

<p>Lösungsversuch mit libssl-dev:</p>

<p>{% codeblock libssl-dev installieren lang:sh %}
sudo apt-get install libssl-dev
rvm reinstall 1.9.3
cd octopress
rake generate
{% endcodeblock %}</p>

<p>&hellip; funktioniert.</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Markdown-Testdokument]]></title>
    <link href="http://uli-heller.github.com/blog/2012/05/18/markdown/"/>
    <updated>2012-05-18T08:00:00+02:00</updated>
    <id>http://uli-heller.github.com/blog/2012/05/18/markdown</id>
    <content type="html"><![CDATA[<h1>Markdown-Testdokument</h1>

<p>Dies ist mein Testdokument für Markdown. Ich packe hier einfach alle denkbaren Konstrukte in Markdown rein, damit ich nicht immer wieder neu suchen muß.</p>

<!-- more -->


<p>Quellen:</p>

<ul>
<li><a href="http://daringfireball.net/projects/markdown/">DaringFireball</a> &ndash; (besser lesbare lokale <a href="{{%20root_url%20}}/articles/daringfireball-markdown.html">Kopie</a> davon)</li>
</ul>


<h2>Überschrift</h2>

<h2>Noch eine Überschrift</h2>

<h3>Abschnitt 1</h3>

<h3>Abschnitt 2</h3>

<ul>
<li>Eine Liste

<ul>
<li>Geschachtelte Liste</li>
</ul>
</li>
<li>Formatierungen

<ul>
<li><em>fett</em></li>
<li><em>fett</em></li>
<li><strong>fett</strong></li>
<li><strong>fett</strong></li>
<li>Kommando: <code>ls -l</code></li>
</ul>
</li>
<li>Liste mit Code-Block:
      cd /tmp
      ls</li>
</ul>


<p>Ein Bild: <img src="/images/cc-by-sa-3.0_88x31.png" alt="CC-Icon" /></p>

<h2>Octopress-Erweiterungen</h2>

<h3>Codeblock</h3>

<p>{% codeblock Here&rsquo;s an example .rvmrc file. lang:ruby %}
rvm ruby-1.8.6 # ZOMG, seriously? We still use this version?
{% endcodeblock %}</p>

<p>Nach gegenwärtigem Kenntnisstand kann so ein Codeblock nicht Bestandteil einer Liste sein! (Octopress-2.0 und 2.1)</p>

<h3>Datei</h3>

<p>{% include_code Beispiel.java %}</p>

<h3>Bild</h3>

<p>{% img /images/cc-by-sa-3.0_88x31.png &lsquo;CC-Icon&rsquo; %}</p>

<h2>Tests</h2>

<h3>Liste mit Codeblock</h3>

<ul>
<li>Liste</li>
<li>mit Codeblock
  {% codeblock Here&rsquo;s an example .rvmrc file. lang:ruby %}
  rvm ruby-1.8.6 # ZOMG, seriously? We still use this version?
  {% endcodeblock %}</li>
</ul>

]]></content>
  </entry>
  
</feed>
